import styled from 'styled-components';

const InputAlternativeStyles = styled.input`
    
`;

export const ErrorMessage = styled.small`
    
`;

export const InputWrap = styled.div`
`

export default InputAlternativeStyles;
