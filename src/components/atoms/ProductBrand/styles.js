import styled from "styled-components";

const ProductBrandStyled = styled.span`
  color: #666666;
  font-size: 12px;
  font-style: normal;
  font-weight: normal;
  line-height: 16px;
`;

export default ProductBrandStyled;
